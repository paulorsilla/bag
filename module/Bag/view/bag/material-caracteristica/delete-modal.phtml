<?php
    $title = 'Excluir um registro';
    $url   = $this->url('bag/material-caracteristica', ['action' => 'delete-modal', 'id' => $this->materialCaracteristica->getId()]);
    $this->headTitle($title);
?>
<h1><?php echo $this->escapeHtml($title) ?></h1>

<p>
    Deseja realmente excluir 
    "<?php echo $this->escapeHtml($this->materialCaracteristica->getCaracteristica()->getNomeCurto()); ?>"
</p>

<form action="<?php echo $url; ?>" method="post">
    <div class="form-group">
        <input type="hidden" name="id" value="<?php echo (int) $this->materialCaracteristica->getId(); ?>" />
    </div>
</form>